# Satish Randive Animated Portfolio

Static HTML/CSS/JS portfolio with an animated particle/network hero and animated cloud-data flow.

Deploy by placing `index.html`, `styles.css`, and `script.js` directly in your GitHub repository and importing that repository into Vercel.
