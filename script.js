const c=document.getElementById('canvas'),x=c.getContext('2d');let w,h,n=[];function resize(){w=c.clientWidth;h=c.clientHeight;const d=Math.min(devicePixelRatio||1,2);c.width=w*d;c.height=h*d;x.setTransform(d,0,0,d,0,0);n=Array.from({length:Math.min(50,Math.floor(w/25))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.25,vy:(Math.random()-.5)*.25,r:Math.random()*1.4+.5}))}function draw(){x.clearRect(0,0,w,h);n.forEach(a=>{a.x+=a.vx;a.y+=a.vy;if(a.x<0||a.x>w)a.vx*=-1;if(a.y<0||a.y>h)a.vy*=-1});for(let i=0;i<n.length;i++)for(let j=i+1;j<n.length;j++){let a=n[i],b=n[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<150){x.globalAlpha=(1-d/150)*.16;x.beginPath();x.moveTo(a.x,a.y);x.lineTo(b.x,b.y);x.strokeStyle='#111';x.stroke()}}x.globalAlpha=.28;n.forEach(a=>{x.beginPath();x.arc(a.x,a.y,a.r,0,7);x.fillStyle='#111';x.fill()});x.globalAlpha=1;requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw();document.getElementById('menu').onclick=()=>document.getElementById('nav').classList.toggle('open');document.querySelectorAll('#nav a').forEach(a=>a.onclick=()=>document.getElementById('nav').classList.remove('open'));